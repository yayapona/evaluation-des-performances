import { directionModel } from "./directionModel";

export class sousDirectionModel {
    id!:number;
    code_sous_direction!:number;
    direction!: directionModel;
    libelle_court!: string;
    libelle_long!:string
}